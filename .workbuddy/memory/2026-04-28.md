# 2026-04-28 工作日志

## 设置模块 + 用户管理
- FinanceApp.tsx 侧边栏「设置」改名为「财务设置」
- 主系统 Layout.tsx 合规后面新增「设置」大频道（TopChannel = settings）
- 顶部导航新增「设置」Tab，侧边栏含「登录用户」菜单
- App.tsx 新增 `/settings/users` 路由 → UserSettings 页面
- 创建后端 UserModule（entity/service/controller/auth-controller）
  - users 表：id/username/password/displayName/role/active/createdAt/updatedAt
  - 密码：SHA256 + salt 哈希
  - 启动时自动 seedAdmin：root/123456
- AuthContext 改造：先尝试后端 `/api/auth/login`，失败 fallback 本地硬编码
- **踩坑**: NestJS `setGlobalPrefix('api')` 已加前缀，Controller 不需再写 `api/`，否则变成 `api/api/users` 404

## 服务启动
- 前后端需手动启动，重启电脑后不会自动运行
- 后端修改后需 `npx nest build` 再启动

## 前端模块化重构（完成）

### 页面按领域分文件夹
- 25个页面从 pages/ 平铺 → 按领域分到12个子文件夹：dashboard/customer/finance/voucher/invoice/inventory/asset/report/tax/compliance/settings/auth
- 每个文件内的 ../api → ../../api 等路径已更新
- App.tsx 和 FinanceApp.tsx 的 import 路径已更新

### FinanceApp.tsx 拆分
- 原325行 → 135行粘合层 + 4个独立模块
- config/financeMenuConfig.tsx：侧边栏菜单配置
- config/financeTabConfig.ts：标签页标题配置
- config/useFinanceTabs.ts：标签页管理 Hook
- config/FinanceRouter.tsx：路由→组件映射

### 菜单配置抽出
- config/mainMenuConfig.tsx：管理端顶部频道+侧边栏配置
- Layout.tsx 从159行精简，删除内联菜单配置

### 类型定义集中管理
- 	ypes/voucher.ts：Voucher, VoucherEntry, AccountTitle, AssistantItem
- 	ypes/tenant.ts：Customer, ActiveTenant
- 	ypes/invoice.ts：Invoice
- 	ypes/report.ts：DzBalanceSheetRow, IncomeData, CashFlowData, TaxBurdenData, ComplianceData, DashboardData
- 	ypes/user.ts：UserItem, AuthUser
- 	ypes/index.ts：统一导出
- 旧页面暂未迁移引用，新代码直接从 types/ 引用