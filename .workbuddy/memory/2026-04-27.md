# 2026-04-27


## 登录页面实现
- 新建 src/auth/AuthContext.tsx：AuthProvider + useAuth + 硬编码账号 root/123456
- 新建 src/auth/RequireAuth.tsx：路由守卫，未登录跳转 /login
- 新建 src/pages/Login.tsx：左侧蓝色渐变品牌区 + 右侧白色卡片登录表单
- 更新 src/App.tsx：添加 /login 公开路由，用 RequireAuth 保护所有业务路由
- 更新 src/main.tsx：最外层加 AuthProvider
- 更新 src/components/Layout.tsx：顶栏右侧加用户名+退出登录下拉菜单


## 四大模块补全
- **后端 report 模块新增 4 个 API**：/api/report/cash-flow(现金流量表)、/api/report/tax-burden(税负分析)、/api/report/compliance(合规检查)、/api/report/dashboard(Dashboard统计)
- **前端 Dashboard.tsx** 重写：对接真实API，展示凭证统计、发票处理率、快捷操作入口、数据概览
- **前端 ReportCashFlow.tsx** 新建：经营/投资/筹资三大活动现金流明细+汇总卡片+期末余额
- **前端 TaxBurden.tsx** 重写：增值税负率计算、发票统计、综合税负评估（颜色分级）、风险提示
- **前端 Compliance.tsx** 重写：合规评分仪表盘、5项检查规则（借贷平衡/分录数/摘要/金额/期间）、问题明细表
- **Layout.tsx 侧边菜单** 更新：税务频道拆分为「税负分析」+「现金流量表」，合规频道改为「合规检查」
- **App.tsx 路由** 新增 /tax/burden、/tax/cash-flow、/compliance/check，旧路径兼容重定向
- TypeScript 编译 0 错误，Vite 热更新可用

## 客户数据排查
- **问题**: 客户列表页刷不出数据，API 返回 500
- **根因**: 后端 NestJS 未启动（
pm run start:dev 未运行）
- **解决**: 启动后端服务，端口 3001，连接远程 PG(192.168.31.127:5001)
- **注意**: 后端需手动保持运行，重启电脑后需重新启动

## 账套首页 & finance.html 标签 bug
- **新增**: FinanceHome.tsx 账套首页（进账套后的主页，快捷入口+记账助手+本期财税指标）
- **架构**: FinanceApp 默认路由从 /voucher 改为 /home，侧边菜单增加"账套首页"入口
- **Bug修复**: finance.html 打开时 location.pathname 为 `/finance.html`，被错误创建为标签页。在 useEffect 中增加 `/finance.html` 路径判断，与根路径一样重定向到 `/home`
